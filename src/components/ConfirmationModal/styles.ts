import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 2.5rem;
  display: grid;
  gap: 2.5rem;

  text-align: center;

  min-width: 100%;
  max-width: 35rem;

  > div {
    display: grid;
    gap: 0.5rem;
  }
`;
