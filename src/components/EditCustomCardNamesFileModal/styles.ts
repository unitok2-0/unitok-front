import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 2.5rem;

  text-align: center;
  display: grid;
  gap: 2.5rem;

  width: 33rem;

  @media (max-width: 798px) {
    max-width: 90vw;
  }
`;
